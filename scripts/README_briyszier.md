# Scripts

Logo vectorization / restoration tooling moved to standalone app **briyszier**:
`C:\Users\Brice\Projects\briyszier` (and its own GitHub repo when published).

In-app Flutter logo helpers under `mobile/lib/logo_*.dart` remain for the shipping-label product.
